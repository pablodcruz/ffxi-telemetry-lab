name: ffxi_telemetry
version: 1.0.0
config-version: 2

profile: ffxi_telemetry

model-paths: ["models"]
test-paths: ["tests/dbt"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ffxi_telemetry:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
